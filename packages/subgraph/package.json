{
  "name": "@crypto-next/subgraph",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ GITHUB_USERNAME/your-contract",
    "create-local": "graph create --node http://localhost:8020/ crypto/your-contract",
    "remove-local": "graph remove --node http://localhost:8020/ crypto/your-contract",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 crypto/your-contract"
  },
  "devDependencies": {
    "mustache": "^4.2.0"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.34.0",
    "@graphprotocol/graph-ts": "^0.28.0"
  }
}
